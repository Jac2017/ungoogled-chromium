--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -113,6 +113,7 @@
 #include "base/auto_reset.h"
 #include "base/command_line.h"
 #include "base/containers/contains.h"
+#include "base/command_line.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/i18n/rtl.h"
--- a/chrome/browser/ui/views/frame/tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/tab_strip_region_view.cc
@@ -13,6 +13,7 @@
 #include <utility>
 #include <vector>

+#include "base/command_line.h"
 #include "base/strings/utf_string_conversions.h"
 #include "chrome/browser/themes/theme_properties.h"
 #include "chrome/browser/ui/browser_element_identifiers.h"
@@ -222,6 +223,11 @@ TabStripRegionView::TabStripRegionView(s
   // the tab strip for overflow.
   SetFlexForView(tab_strip_, {1});

+  if (base::CommandLine::ForCurrentProcess()->HasSwitch("vertical-tabs-only")) {
+    SetVisible(false);
+    SetPreferredSize(gfx::Size(0, 0));
+  }
+
   UpdateNewTabButtonBorder();

   SetProperty(views::kElementIdentifierKey, kTabStripRegionElementId);
--- a/chrome/browser/ungoogled_flag_entries.h
+++ b/chrome/browser/ungoogled_flag_entries.h
@@ -88,4 +88,8 @@
      "Hide tab close buttons",
      "Hides the close buttons on tabs. ungoogled-chromium flag.",
      kOsDesktop, SINGLE_VALUE_TYPE("hide-tab-close-buttons")},
+    {"vertical-tabs-only",
+     "Vertical Tabs Only (Hide Tab Strip)",
+     "Hides the horizontal tab strip for use with the Dark Vertical Tabs extension. Requires the extension to be loaded. ungoogled-chromium flag.",
+     kOsDesktop, SINGLE_VALUE_TYPE("vertical-tabs-only")},
 #endif  // CHROME_BROWSER_UNGOOGLED_FLAG_ENTRIES_H_
